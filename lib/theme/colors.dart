import 'package:flutter/material.dart';

final mainColor = Color(0xff0BBD64);
final secondaryColor = Color(0xffFFFFFF);

class AppColors {
  // Core Dark Theme Colors
  static const Color background = Color(0xFF121212);
  static const Color card = Color(0xFF151515);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color primary = Colors.red;
  static const Color secondary = Colors.redAccent;

  // Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;

  // Button & Icon Colors
  static const Color button = Colors.red;
  static const Color icon = Colors.white;

  // Border & Divider
  static const Color divider = Colors.white24;

  // Error
  static const Color error = Colors.redAccent;

  // ignore: prefer_typing_uninitialized_variables
  static var accent;
}
